name: 05fx
on: 
  push:
  schedule:
    - cron: '0 8 3 * *'
env:
  FLY_API_TOKEN: ${{ pyOXOfLxmS4AtQobc1AymSTP-t_0fRODxq9zpitr8Is }}
  APP_NAME: ${{ 05fx }}
  SQLUSER: ${{ mysql }}
  DATABASE: ${{ secrets.DATABASE }}
  SQLPASSWORD: ${{ secrets.SQLPASSWORD }}
  SQLHOST: ${{ secrets.SQLHOST }}
  SQLPORT: ${{ secrets.SQLPORT }}
  SQLNAME: ${{ secrets.SQLNAME }}
jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Deploy
        run: |
          sh ./deploy.sh
